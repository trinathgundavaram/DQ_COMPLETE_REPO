# Python >= 3.8 required
# ── Metadata store (always required) ─────────────────────────────────────────
teradatasql>=17.20.0.0

# ── Source system drivers (install only what you need) ───────────────────────

# PostgreSQL / AWS Aurora (PostgreSQL-compatible)
psycopg2-binary>=2.9.0

# Databricks SQL Warehouses
databricks-sql-connector>=3.0.0

# Microsoft SQL Server (requires ODBC Driver for SQL Server on the host OS)
pyodbc>=5.0.0

# ── Flat-file sources (CSV, Excel, TSV, Parquet) ─────────────────────────────
duckdb>=0.10.0
pandas>=2.0.0
openpyxl>=3.1.0       # .xlsx support
xlrd>=2.0.0           # .xls (legacy Excel) support
pyarrow>=14.0.0       # .parquet support (optional — only needed for Parquet files)

# ── Resilience ───────────────────────────────────────────────────────────────
tenacity>=8.2.0       # exponential-backoff retry for transient source-DB errors

# ── S3 landed-file sources ─────────────────────────────────────────────────
# duckdb (already listed above) provides httpfs for S3 Parquet/CSV reads.
boto3>=1.34.0          # optional — only needed if resolving credentials via
                        # boto3 session chain rather than instance/task role

# ── Testing ──────────────────────────────────────────────────────────────
pytest>=7.4.0

# ── Local dev credentials (shared/) ───────────────────────────────────────
# Loads dev.env (see dev.env.example) into the process env at import time;
# db/adapters.py / db/connection_factory.py are unmodified and unaware of
# this -- they just see already-populated DQ_<NAME>_* env vars either way.
python-dotenv>=1.0.0
